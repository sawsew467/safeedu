import ProfileUserScreen from "@/components/features/Account/profile-user-screen";
import React from "react";
import { View } from "react-native";

const Profile = () => {
  return <ProfileUserScreen />;
};

export default Profile;
